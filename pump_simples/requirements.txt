# robozão — dependências

# --- Necessário sempre (dashboard + modo DRY_RUN) ---
requests>=2.31
flask>=3.0

# --- Necessário SÓ para modo REAL (assinar/enviar transações Solana) ---
# Só base58 (puro Python, sem compilação nenhuma). A assinatura ed25519 é
# feita pelo módulo próprio `ed25519_pure.py` (stdlib only) — de propósito,
# depois de solders (Rust/PyO3) e pynacl (C/libsodium) terem ambos falhado
# a compilar no Termux. base58 nunca teve esse problema (é wheel puro).
base58>=2.1
