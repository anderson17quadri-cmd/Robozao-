# robozão — dependências

# --- Necessário sempre (dashboard + modo DRY_RUN) ---
requests>=2.31
flask>=3.0

# --- Necessário SÓ para modo REAL (assinar/enviar transações Solana) ---
# Podes instalar só quando fores mesmo trocar para real.
# PyNaCl (ed25519) + base58 — leves, sem compilação Rust (ao contrário do
# solders, que falha facilmente no Termux, sobretudo em Python muito recente).
pynacl>=1.5
base58>=2.1
